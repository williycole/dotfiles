-- NOTE: Biome approach if you have it installed via mason and lazyextras
-- this doesn't fully work
-- return {
--   {
--     "stevearc/conform.nvim",
--     ---@type conform.setupOpts
--     opts = function(_, opts)
--       -- Ensure Biome is configured with the custom config path
--       opts.formatters_by_ft = opts.formatters_by_ft or {}
--       for _, ft in ipairs({ "javascript", "javascriptreact", "typescript", "typescriptreact" }) do
--         opts.formatters_by_ft[ft] = opts.formatters_by_ft[ft] or {}
--         table.insert(opts.formatters_by_ft[ft], "biome")
--       end
--       opts.formatters = opts.formatters or {}
--       opts.formatters.biome = {
--         command = "biome",
--         args = { "format", "--stdin-file-path", "$FILENAME", "--config-path", "/home/cboren/.config/nvim/biome.json" },
--         stdin = true,
--       }
--       -- Keep other formatters
--       opts.formatters_by_ft.json = { "prettier" }
--       opts.formatters_by_ft.yaml = { "prettier" }
--       opts.formatters_by_ft.lua = { "stylua" }
--       opts.format_on_save = {
--         timeout_ms = 1000,
--         lsp_fallback = false,
--       }
--       opts.log_level = vim.log.levels.DEBUG
--       opts.notify_on_error = true
--     end,
--   },
--   {
--     "neovim/nvim-lspconfig",
--     opts = {
--       servers = {
--         eslint = { format = false },
--         vtsls = { format = false },
--       },
--     },
--   },
--   {
--     "nvimtools/none-ls.nvim",
--     opts = {
--       sources = {
--         require("null-ls").builtins.formatting.goimports,
--         require("null-ls").builtins.formatting.gofumpt,
--       },
--     },
--   },
-- }
return {}
